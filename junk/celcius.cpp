#include <iostream>
void count() { std::cout << "Nothing"; }
void line() { std::cout << "Fuck"; }
void foo() {}
int main() {
  double c;
  std::cout << "enter the amount of energy in Celcius: ";
  std::cin >> c;
  std::cout << "the farenheit is :" << (c * 9 / 5) + 32;
  // just kinda learning git hmmm
  // still lookint the changes and learning them all
  return 0;
}

